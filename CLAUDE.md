# CLAUDE.md — 스킬 작성 가이드

이 리포는 박기린(op5321)의 개인 Claude Code 스킬 모음입니다.
**새 스킬을 추가하거나 기존 스킬을 수정할 때는 아래 스킬 작성 도구를 거쳐서 작성**하세요.
스킬 도구 없이 임의로 짜지 말 것.

## 세션 시작 규칙 (필수)

새 에이전트(세션)가 켜질 때마다, **사용자 요구에 착수하기 전에 이 리포 안의 모든 스킬을 먼저 읽는다.**

1. 리포 루트의 각 스킬 디렉토리 `*/SKILL.md`를 전부 훑어 어떤 스킬이 있고 무엇을 하는지(트리거·절차·관련 스크립트) 파악한다. (`ls */SKILL.md` 후 각 frontmatter+개요 확인)
2. 그다음 사용자의 요구를 **기존 스킬·스크립트에 매핑**해서 제시·처리한다. 이미 스킬이 처리하는 일을 손으로 재발명하지 말 것 — 예: 티스토리→네이버는 `migrate.py`가 글쓰기 창을 알아서 띄우고(`ensure_postwrite_tab`) 카테고리·발행까지 스크립트가 있다. "창 열어 주세요" 같은 퇴화 금지.
3. 요구가 기존 스킬로 안 되면, 그때 위 "스킬 작성 도구"로 새 스킬/보강을 만든다.

요지: **읽고(스킬 인벤토리) → 매핑(요구를 스킬에) → 실행**. 기억에 의존해 스킬 동작을 추측하지 말고 SKILL.md·스크립트를 실제로 확인한다.

## 사용할 도구 (우선순위)

1. `/oh-my-claudecode:skillify` — 현재 세션의 반복 워크플로를 스킬 초안으로 추출
2. `/oh-my-claudecode:skill` — 로컬 스킬 추가·삭제·검색·수정 위저드
3. `skill-creator` (Anthropic 공식) — 처음부터 만들거나 description 최적화
4. `superpowers:writing-skills` — 작성·검증 일반 프로세스

이유: frontmatter 규칙·trigger description·디렉토리 구조를 도구가 강제하므로,
"trigger 약한 스킬", "scripts 없는 반쪽 스킬" 같은 실수를 방지.

## 하드룰: 글쓰기

**적용 범위는 모든 산출물이다.** 블로그, Notion 문서, SKILL.md, 발표자료, 그리고 대화창 답변까지 전부 포함한다.
사용자가 "쓰지 말라"고 한 표현은 예외 없이 영구 금지다. 로컬 메모리가 아니라 이 파일에 기록해 리포에 영속화한다.

### 절대 금지

- **이모지.** 한 글자도 쓰지 않는다. Notion callout 아이콘처럼 블록 속성으로 들어가는 이모지도 포함한다.
- **em dash (`—`).** 제목, 불릿 정의, 코드 주석, 참고문헌 어디에도 쓰지 않는다. 콜론(`:`)이나 쉼표로 바꾸거나 문장을 나눈다. (2026-07-14)
- **"~지점입니다" 상투구.** "역할 폭발이 사라지는 지점입니다", "ABAC이 어려워하는 지점이 바로 이것입니다" 같은 문장. 그냥 직접 서술한다("역할 폭발이 여기서 사라집니다"). 단 PEP/PDP처럼 Point의 번역어로 쓰는 기술 용어는 예외. (2026-07-14)
- **다른 장을 가리키는 문장.** 앞이든 뒤든 마찬가지다. "7장에서 다시 다룹니다", "4장의 PEP가 여기 해당합니다", "3장에서 표현하지 못했던", "뒤에 볼 ABAC" 전부 금지. 각 문단은 그 자리에서 자족적으로 읽혀야 한다. 필요하면 개념 이름을 그대로 다시 쓴다("RBAC이 표현하지 못했던"). (2026-07-14)
- **애매한 비유.** "관리 대상이 곱셈에서 덧셈으로 떨어집니다" 같은 수사는 이해를 돕는 게 아니라 헷갈리게 만든다. 비유 대신 구체적인 숫자와 예시로 쓴다. (2026-07-14)
- **늘어지는 종결.** "~라는 사실 하나입니다", "~라는 점입니다" 같은 군더더기로 문장을 늘리지 않는다. `~입니다`로 끝낼 거면 짧고 깔끔하게 끝낸다. (X) "결과물은 이 요청의 주인은 user_42라는 사실 하나입니다." (O) "인증을 통과하면 이 요청의 주인이 user_42임이 확정됩니다." (2026-07-14)
- **"정확히 여기입니다" / "바로 이것입니다" 말투.** "RBAC과 ABAC이 다루는 영역이 정확히 여기입니다", "1위 항목이 바로 이것입니다", "ABAC이 바로 이것을 어려워합니다" 전부 금지. 손가락질하듯 강조하지 말고 그냥 서술한다. ("RBAC과 ABAC은 이 판단을 다룹니다.") (2026-07-14)
- **"~의 핵심입니다" / "핵심은 ~라는 것입니다" 말투.** "이것이 이 패턴의 핵심입니다", "핵심은 권한을 관계 튜플로 표현한다는 것입니다", "세 번째 방식이 핵심입니다" 전부 금지. 무엇이 중요한지 선언하지 말고 그냥 그것을 서술한다. ("이렇게 하면 권한이 엔드포인트마다 한 줄로 선언됩니다.") (2026-07-14)
- **정보 없는 추임새·메타 문장.** 방금 한 말을 되짚거나 가리키기만 하는 문장을 덧대지 않는다. "바로 그 점이 이 패턴이 동작하는 이유입니다", "이것이 중요합니다", "여기서 알 수 있습니다" 같은 문장은 설명을 더하지 않고 분량만 늘린다. 왜 중요한지를 선언하지 말고 내용 자체로 설명한다. (2026-07-15)
- **영어 기술 용어의 기계적 직역.** row-level을 "행 단위", coarse-grained를 "거친 권한"으로 옮기면 한국어로는 뜻이 잡히지 않는다. 무엇을 검사하고 무엇을 하는지로 풀어 쓴다. "이 기능을 쓸 수 있는지", "이 글을 건드려도 되는지". (2026-07-14)
- **번역서 말투.** "대가는 ~라는 점입니다"라고 쓰지 않는다. 개발자가 실제로 쓰는 말인 **트레이드오프**로 쓴다. (2026-07-14)
- **한 줄로 넘기는 결론.** "TypeScript 타입 안전성이 약합니다" 같은 문장으로 끝내지 말고, 그래서 실제로 무슨 일이 벌어지는지 한두 줄을 반드시 붙인다. (오타가 컴파일에서 안 잡히고 런타임에 false만 돌아와서, 권한이 없어서 막힌 건지 오타 때문인지 구분이 안 된다.) (2026-07-14)
- **당연한 것을 트레이드오프라고 치지 않는다.** "정책 언어를 새로 배워야 한다", "설정 문법을 익혀야 한다"는 어떤 도구를 쓰든 당연하므로 단점 목록에 넣지 않는다. 진짜 트레이드오프(네트워크 왕복, 운영할 시스템 증가, 타입 안전성 상실)만 적는다. (2026-07-14)
- **약어와 고유명사를 설명 없이 던지지 않는다.** CNCF, Zanzibar, NIST처럼 처음 나오는 이름은 그 자리에서 무엇인지, 왜 언급하는지 한 줄로 풀어준다. 이름만 적고 넘어가면 대충 쓴 글이다. (2026-07-14)
- **라이브러리를 소개할 때는 예시 코드를 붙인다.** 이름과 설명만으로는 감이 오지 않는다. 각 도구 문단마다 실제 사용 코드를 한 조각씩 넣는다. (2026-07-14)
- **코드가 먼저, 설명이 나중이다 (2026-07-14).** 설명 문단을 쓰고 그 아래 코드를 붙이는 순서로 쓰지 않는다. 소제목 바로 아래에 코드를 보여주고, 설명은 코드 밑에 쓴다. 독자는 코드를 먼저 보고 나서 그것이 무엇인지 읽는다.
- **번역투.** 영어 문장을 그대로 옮긴 듯한 문장은 한국인이 쓰는 말이 아니다. "ReBAC이 필요한 신호는 분명합니다", "여기서 결정적인 관찰이 하나 나옵니다", "이것이 실무에서 아프게 다가오는 순간이 두 곳입니다" 같은 문장 금지. 한국어로 자연스럽게 쓴다. "다음 중 하나라도 해당되면 ReBAC을 검토해야 합니다", "실무에서는 두 곳에서 문제가 됩니다". (2026-07-14)
- **극적 서두 + 문장 쪼개기.** 결론을 뒤로 미루려고 "~는 오직 하나입니다 / 원인은 하나입니다 / 뿌리는 하나입니다 / ~가 있습니다"라는 서두를 던진 뒤, 그 정체를 다음 문장에서 밝히는 구성을 쓰지 않는다. 한 문장에 담는다. (X) "RBAC이 표현할 수 있는 문장은 오직 하나입니다. 이 역할은 이 권한을 가진다." (O) "RBAC은 '이 역할은 이 권한을 가진다'는 문장만 표현할 수 있습니다." (X) "원인은 하나입니다. 권한 데이터가 앱 DB 밖에 있기 때문입니다." (O) "권한 데이터가 앱 DB 밖에 있어서 생기는 문제입니다." (2026-07-15)
- **업계 은어와 뭉뚱그린 표현.** "현업이 검토 가능한가" 같은 말은 누가 무엇을 하는지 감추기만 한다. 구체적인 사람과 행위로 쓴다. "코드를 못 읽는 사람도 권한을 확인할 수 있는가", "팀장이나 보안 담당자가 역할 목록을 읽고 판단할 수 있는가". (2026-07-14)
- **섹션 제목의 따옴표.** 제목은 짧고 명료하게. 대사와 인용은 본문 안에서만.
- **고유명사 왜곡.** 행사명, 브랜드명, 표준명을 임의로 변형하지 않는다. NIST SP 800-162, ANSI/INCITS 359, OpenFGA 등 원표기 그대로.
- **대화창 답변의 구분선과 표.** 마크다운 구분선 `---`과 표를 대화 답변에 쓰지 않는다. 구분선은 `─`(U+2500)로 렌더되는데 이 문자는 East Asian Ambiguous라 폭이 1칸일 수도 2칸일 수도 있다. Claude Code TUI와 터미널의 계산이 어긋나면 줄이 밀려 겹치고 같은 문장이 두 번 찍힌다. 한글이 섞이면 2칸 문자가 많아 오차가 빨리 누적된다. 표 테두리도 같은 박스 문자라 똑같이 깨진다. 한 줄에 한글과 영어를 길게 섞지 말고 줄을 짧게 끊는다. 화면이 깨졌을 때는 사용자에게 `Ctrl+L`로 다시 그리라고 안내하고, 세션 중 창 크기 변경도 원인이라고 알린다. 마크다운 파일 안에서는 표를 써도 된다. 이 규칙은 대화 출력에만 적용된다. (2026-07-28 Orca 터미널 실측)

### 필수

- **기본 보이스는 명료한 분석글.** 군더더기 없는 설명체, 정보 전달 우선.
- **모든 문장을 `~입니다` / `~합니다` 체로.** 구어체 종결어미와 감탄사 금지. "~다" 체로 쓰지 않는다.
- **말투 모방 금지.** 코퍼스로 사용자 말투를 흉내내는 것은 2026-06-16 피드백으로 폐기됐다. 애매하게 따라하면 안 쓰니만 못하다.
- **노트 이관은 창작이 아니라 전사.** 사용자가 써둔 글을 옮길 때는 종결어미만 바꾸고 내용을 지어내지 않는다.
- 한국어 톤. SKILL.md 본문도 한국어 우선.
- 코퍼스 의존 스킬은 부재 시 `/blog-learn` 친절 안내.

## 하드룰: 도구 입력의 한글

**한글은 리터럴 텍스트로 직접 입력한다. `\u` 이스케이프로 손 계산해서 넣지 않는다.**

Notion MCP, 파일 쓰기, osascript 주입 어디서든 마찬가지다. 코드포인트를 직접 계산하면 한 글자씩 어긋난다. 사고 전례가 계속 쌓이고 있다.

- 2026-07-06: "챕터8"이 "챕터엘"로, "딱"이 "땅"으로, "씀"이 "쓀"로 저장됨
- 2026-07-28: "잇는"이 "잉는"으로 저장됨
- 2026-07-29: "떼면"이 "뗎면"으로, "바뀐다"가 "바뀜다"로 저장됨

저장 후에는 반드시 fetch로 다시 읽어 고유명사와 조사를 눈으로 확인한다. 깨졌으면 즉시 update로 고친다. 관련 메모리: [[notion-korean-literal-no-escape]].
